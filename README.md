# Web Image Filter (React + Vite)

App para cargar una imagen, **pintar una máscara**, aplicar **suavizado selectivo** en WebGL y **exportar PNG**. Incluye modo **Split** arrastrable.

## Requisitos
- Node.js 18+ y npm

## Ejecutar
```bash
npm install
npm run dev